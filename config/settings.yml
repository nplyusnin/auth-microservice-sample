app:
  name: auth
db:
  adapter: postgresql
  host: localhost
  user: postgres
  password: postgres
pagination:
  page_size: 10
app:
  secret: 'my_super_secret'
logger:
  path: log/app.log
  level: info